# Tests must not inherit a developer's local .env. Without this, setting a real
# VITE_MAPBOX_TOKEN locally makes RadarView render the Mapbox map instead of the
# schematic, and every schematic test fails on a machine that has a token.
# Specs that need the map path stub import.meta.env themselves.
VITE_MAPBOX_TOKEN=
