# BE-6 — Dispatcher Comms — Report

Status: DONE

Tests: `npx vitest run` → 26 files, 166 passed (151 baseline + 15 new).
Typecheck: `npx tsc --noEmit` → 0 errors.

Commits (fleet-backend/ scoped only):
- 2148f7b — feat: add dispatcher comms routes for BE-6
- f5706cf — feat: emit general_notification for dispatcher messages and driver notify
- 2465c77 — test: cover dispatcher comms routes and realtime notification delivery

Concerns: none. `GET /dispatcher/conversations` returns the full last-message row
(not just its text) under `lastMessage` — a reasonable default per existing
response-shape conventions in the codebase, but flagging in case the portal
expects a flattened string.
