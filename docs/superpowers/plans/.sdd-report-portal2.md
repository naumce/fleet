# Portal-2 — Fleet Management — SDD Report

**Status:** DONE

## Verification
- `npx vitest run`: **8 test files, 37 tests passed** — Portal-1's original 10 + 27 new (4 `DataTable.spec.ts`, 7 `drivers.spec.ts`, 8 `vehicles.spec.ts`, 3 `DriversView.spec.ts`, 5 `VehiclesView.spec.ts`).
- Raw vitest summary: `Test Files 8 passed (8)` / `Tests 37 passed (37)`.
- `npx vue-tsc --noEmit`: 0 errors.
- `npm run build` (`vue-tsc -b && vite build`): succeeds. One pre-existing, unrelated warning (`INEFFECTIVE_DYNAMIC_IMPORT` on `src/stores/auth.ts`, present before this work, originating in `src/lib/api.ts`'s lazy import of the auth store).

## Commits (all scoped to `fleet-portal/`, staged with explicit `git add`, no `-A`)
1. `0012b91` — feat: add reusable component kit for fleet management screens (DataTable, Modal, FormField, AppButton + DataTable.spec.ts)
2. `bcef832` — feat: add drivers and vehicles Pinia stores (+ specs)
3. `c6c241f` — feat: add DriversView with create/edit and activate Drivers nav link (+ route, spec)
4. `b9bf4f8` — feat: add VehiclesView with create/edit/assign, activate Vehicles nav link (+ route, spec; also fixed a `findButtonByText` helper type in both view specs that `vue-tsc -b`'s project build caught but `vue-tsc --noEmit` alone did not)

## What was built
- `src/components/{DataTable,Modal,FormField,AppButton}.vue` — small, prop-driven, reused by both views.
- `src/types/fleet.ts` — `Driver`/`Vehicle` types + create/update payload types.
- `src/stores/{drivers,vehicles}.ts` — Pinia stores (`list/create/update`, `vehicles` also `assign`), following the existing `auth.ts` pattern (loading/error state, `extractApiErrorMessage`, refresh-on-success).
- `src/views/DriversView.vue` — DataTable + create/edit modal.
- `src/views/VehiclesView.vue` — DataTable + create/edit modal + a separate assign modal with a driver `<select>` sourced from `useDriversStore`.
- `src/router/index.ts` — added `/drivers` and `/vehicles` child routes under `AppShell`.
- `src/layouts/AppShell.vue` — activated the previously-disabled Drivers/Vehicles sidebar links.

## Concerns
- None blocking. Minor note: `Modal.vue` teleports to `document.body`; the view specs query it via a `DOMWrapper(document.body)` helper and clear `document.body.innerHTML` in `afterEach` to avoid cross-test leakage — worth keeping in mind if more Modal-based specs are added later.
