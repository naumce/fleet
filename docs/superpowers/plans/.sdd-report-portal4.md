# Portal-4 — Messaging + Live Tracking — Implementation Report

**Status:** DONE

## Verification
- `npx vitest run`: 17 test files, **97 passed** (Portal-3's 78 + 19 new: 7 in `stores/messages.spec.ts`, 5 in `stores/tracking.spec.ts`, 4 in `views/MessagesView.spec.ts`, 3 in `views/TrackingView.spec.ts`).
- `npx vue-tsc --noEmit`: 0 errors.
- `npm run build`: succeeds (194.72 kB JS, 11.89 kB CSS). One pre-existing `INEFFECTIVE_DYNAMIC_IMPORT` warning for `src/stores/auth.ts` (already dynamically+statically imported before this increment) — unrelated to this work, not a new warning.

## Commits
1. `3975f79` — feat: add messages and tracking stores (`types/dispatcher.ts`, `stores/messages.ts`, `stores/messages.spec.ts`, `stores/tracking.ts`, `stores/tracking.spec.ts`)
2. `ef677ab` — feat: add messaging view and activate Messages route (`views/MessagesView.vue`, `views/MessagesView.spec.ts`, `router/index.ts`, `layouts/AppShell.vue`)
3. `ba20635` — feat: add live tracking view and activate Tracking route (`views/TrackingView.vue`, `views/TrackingView.spec.ts`, `router/index.ts`, `layouts/AppShell.vue`)

All three commits touch only `fleet-portal/` paths, staged explicitly (no `git add -A`).

## What was built
- `src/types/dispatcher.ts` — added `Message`, `Conversation` (with `lastMessage: Message | null`), `DriverLocation`.
- `src/stores/messages.ts` — `useMessagesStore`: `listConversations()`, `openThread(conversationId)`, `send(conversationId, text)` (posts then reloads the thread). Same loading/error pattern as `approvals.ts`/`drivers.ts`.
- `src/stores/tracking.ts` — `useTrackingStore`: `listLocations()`, `startPolling(intervalMs=8000)` (fetches immediately, then on a `window.setInterval`; clears any prior timer first), `stopPolling()`.
- `src/views/MessagesView.vue` — two-pane layout: conversation list (driver name, `lastMessage?.text` preview with a "No messages yet." fallback for null, unread badge) on the left; selected thread (bubbles aligned by `senderType`, dispatcher right/primary blue, driver left/gray) + `FormField`/`AppButton` composer on the right, wired to `openThread`/`send`.
- `src/views/TrackingView.vue` — `DataTable` of latest per-driver locations (name, lat/lng, speed, "Ns/Nm ago"), active-driver-count header, `startPolling()` on mount / `stopPolling()` on `onBeforeUnmount`.
- `src/router/index.ts` — added `/messages` (`messages`) and `/tracking` (`tracking`) under the authenticated `AppShell` layout.
- `src/layouts/AppShell.vue` — both sidebar links activated (`Messages`, `Tracking` — the latter newly added since Portal-3 had no tracking entry at all).

## Concerns / notes
- **No Leaflet map was added.** Per the plan, the map was optional and only worth adding "if it doesn't risk the build" — the polling `DataTable` board is the required deliverable and is fully implemented; a map was skipped to keep the increment low-risk for the final portal build.
- Everything else matches the plan and existing conventions (Modal specs clearing `document.body.innerHTML` in `afterEach` — used in `MessagesView.spec.ts`/`TrackingView.spec.ts`; `RouterLinkStub` for specs rendering `RouterLink` — used in `MessagesView.spec.ts`). No blockers encountered.
