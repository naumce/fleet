# docker-compose secrets — copy to .env or export before `docker compose up`.
JWT_ACCESS_SECRET=      # required — strong random secret
JWT_REFRESH_SECRET=     # required — strong random secret
POSTGRES_PASSWORD=fleet # optional — defaults to "fleet" for local use
